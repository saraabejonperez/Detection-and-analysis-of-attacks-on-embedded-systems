services:

  api:
    build: ./web_app
    container_name: dos_detector_api

    ports:
      - "5000:5000"

    volumes:
      - ./web_app:/app
      - ./web_app/storage/db:/app/instance

    restart: unless-stopped